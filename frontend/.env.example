# Added by the Vercel "Upstash for Redis" integration when you connect a database.
# Without these set, the board falls back to in-memory dummy data (no persistence).
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Required for the AI chat sidebar. Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=
